#!/usr/bin/env python3
#-*- coding:utf-8 -*-
"""
 sort(key, reverse=False)
"""
当用户需要一个排好的列表副本，同时又保留原有列表不变的时候
>>>x = [2, 4, 1, 8, 23, 12]
>>>y = x
>>>
>>>y = x.sort()
>>>print(y)
  None
简单的赋值实际得到的列表副本是None，原因是x.sort()修改了列表后，实际得到的是已经排序的x和None型的y。

正确的做法是:
>>>x = [2, 4, 1, 8, 23, 12]
>>>y = x[:]   #进行切片操作，创建副本，x也不会改变
>>>y.sort()
>>>print(y)

另外一种方法，直接使用sorted函数：
sorted()总是返回一个列表
>>>x = [2, 4, 1, 8, 23, 12]
>>>y = sorted(x)
>>>y
...[1, 2, 4, 8, 12, 23]
>>>x
...[2, 4, 1, 8, 23, 12]
